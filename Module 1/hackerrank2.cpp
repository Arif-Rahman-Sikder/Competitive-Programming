#include <bits/stdc++.h>
//[-(~TheHonestGuy~)-]
using namespace std;
int main()
{
    int a, b;
    cin >> a >> b;
    int eqn = (a * a) + (3 * a) + (2 * b) - (2 * a * b) + (b * b);
    cout<<eqn<<endl;
    return 0;
}