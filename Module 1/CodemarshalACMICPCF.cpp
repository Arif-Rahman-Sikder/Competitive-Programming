#include <bits/stdc++.h>
//[-(~TheHonestGuy~)-]
using namespace std;
int main()
{
    int p, q, y;
    cin >> p >> q >> y;
    int ans = y * (52 * ((5 * p) + (2 * q)));
    cout << ans << endl;
    return 0;
}